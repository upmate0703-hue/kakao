# PowerShell 스크립트는 UTF-8 BOM 을 유지해야 합니다. git 이 내용을 바꾸지 않도록 둡니다.
*.ps1  text eol=crlf
*.cmd  text eol=crlf
*.md   text eol=lf
*.yml  text eol=lf

# 맥용 스크립트는 반드시 LF 로 유지해야 실행됩니다.
mac/*.command text eol=lf
mac/*.js      text eol=lf
mac/*.md      text eol=lf
